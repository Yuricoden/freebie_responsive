mixin products-item(...items)
	.product(class="product_#{attributes.class}")
		each item in items
			img.products-item__img(src= "./" + item.img)
			.product-item__name=item.name
			.product-item__price=item.price













